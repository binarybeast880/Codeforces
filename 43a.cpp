#include <bits/stdc++.h>
using namespace std;
main()
{
    int t;
    cin >> t;
    string s[t][20];
    for (int i = 1; i <= t; i++)
    {
        scanf("%s", s[i]);
    }
    sort(s, s + t);
    string ans1 = s[0];
    string ans2 = s[t];
    for (int)
}